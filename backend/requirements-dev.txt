# Development & Testing Dependencies
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2                      # async test client for FastAPI
pytest-cov==5.0.0                  # coverage reporting
factory-boy==3.3.1                 # test fixtures

# Code Quality
ruff==0.6.9                        # ultra-fast linter (replaces flake8, isort)
mypy==1.11.2                       # static type checking
pre-commit==3.8.0                  # git hooks

# Database (testing)
aiosqlite==0.20.0                  # SQLite async backend for tests
